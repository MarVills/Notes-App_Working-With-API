class NoteForListing {
  final noteID;
  final noteTitle;
  final createDateTime;
  final latestEditedDateTime;

  NoteForListing({
    this.noteID,
    this.noteTitle,
    this.createDateTime,
    this.latestEditedDateTime,
  });
}
